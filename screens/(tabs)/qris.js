import { View, Text, StatusBar } from "react-native";
import React from "react";

const Qris = () => {
  return (
    <View
      style={{
        flex: 1,
        alignContent: "center",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>QRIS</Text>
    </View>
  );
};

export default Qris;
